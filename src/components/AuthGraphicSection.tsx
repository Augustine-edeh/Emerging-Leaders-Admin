const AuthGraphicSection = () => {
  return (
    <div
      className="w-full h-full bg-cover bg-center"
      style={{ backgroundImage: "url('/path-to-your-image.jpg')" }}
    >
      {/* Optional: Add overlay, text, or logos here */}
    </div>
  );
};

export default AuthGraphicSection;
